# Debi-Dorshon Client App

This directory is reserved for the frontend / mobile client application (e.g., React Native, Flutter, React, Next.js).

The server backend runs independently in the `../server/` directory.
